import { styled, Container } from "@mui/material";

export const ContainerSectionStyle = styled(Container)({
    margin: "auto",
  });

export const ContainerSlideStyle = styled(Container)({
  backgroundColor: "transparent",
  width: "100%",
});
